apiVersion: apps/v1
kind: Deployment
metadata:
  name: fitness-sudoku
  namespace: ga-pipeline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fitness-sudoku
  template:
    metadata:
      labels:
        app: fitness-sudoku
    spec:
      containers:
        - name: fitness-sudoku
          image: fitness-sudoku
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: fitness-sudoku
  namespace: ga-pipeline
spec:
  selector:
    app: fitness-sudoku
  ports:
    - port: 8000
      targetPort: 8000