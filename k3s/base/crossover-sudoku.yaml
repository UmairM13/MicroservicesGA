apiVersion: apps/v1
kind: Deployment
metadata:
  name: crossover-sudoku
  namespace: ga-pipeline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crossover-sudoku
  template:
    metadata:
      labels:
        app: crossover-sudoku
    spec:
      containers:
        - name: crossover-sudoku
          image: crossover-sudoku
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: crossover-sudoku
  namespace: ga-pipeline
spec:
  selector:
    app: crossover-sudoku
  ports:
    - port: 8000
      targetPort: 8000