apiVersion: apps/v1
kind: Deployment
metadata:
  name: generator-sudoku
  namespace: ga-pipeline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: generator-sudoku
  template:
    metadata:
      labels:
        app: generator-sudoku
    spec:
      containers:
        - name: generator-sudoku
          image: generator-sudoku
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: generator-sudoku
  namespace: ga-pipeline
spec:
  selector:
    app: generator-sudoku
  ports:
    - port: 8000
      targetPort: 8000